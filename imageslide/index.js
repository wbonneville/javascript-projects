const nextImg = document.querySelector('.nextBtn')
const prevImg = document.querySelector('.prevBtn')

nextImg.addEventListener('click', nextSlide)
prevImg.addEventListener('click', prevSlide)

function nextSlide () {
    
}